# Banner and Theme Menu Verification

The desktop preview shows the official logo removed from the top banner and replaced by a realistic Botswana business/investment landscape background. The exact INVEST IN BOTSWANA headline remains prominent with Botswana flag-colored bands inside the letterforms. The three-dot display control is visible at the top-right of the banner.

The mobile preview preserves the full-width banner, keeps the headline readable across the compact width, and positions the three-dot display control inside the top-right corner without breaking the hero layout. The bottom-left theme control is no longer present.

The generated background is referenced through the project-lifecycle storage URL and is layered with a dark text-safe overlay for contrast.
